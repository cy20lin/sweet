function(@command@)
  log_actual("command=@command@")
  log_actual("ARGC=${ARGC}")
  math(EXPR end "0${ARGC} - 1")
  if(NOT "${end}" LESS 0)
    foreach(i RANGE "${end}")
      log_actual("ARGV${i}=${ARGV${i}}")
    endforeach()
  endif()
  log_actual("")
endfunction()
